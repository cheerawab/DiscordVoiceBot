# Discord Bot Token（必填）
DISCORD_TOKEN=

# Whisper 模型大小：tiny / base / small / medium / large / turbo
WHISPER_MODEL=base

# 辨識語言（預設中文），可改為 en / ja 等
WHISPER_LANGUAGE=zh

# （可選）指定輸出辨識結果的文字頻道 ID，留空則使用下指令的頻道
TEXT_CHANNEL_ID=

COMPUTE_TYPE=float16
WHISPER_DEVICE=auto

# （可選）Whisper 提示詞檔案路徑，預設為 prompt.txt
# 機器人會在每次辨識前讀取此檔案內容作為 initial_prompt
WHISPER_PROMPT_FILE=prompt.txt

WEB_PORT=8080
WEB_HOST=0.0.0.0